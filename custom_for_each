Array.prototype.customForEach = function(callback){
    for(let i=0;i<this.length;i++){
        callback(this[i],i,this)
    }
}
const fruits = ['apple','bannana','cherry']
fruits.customForEach(item =>console.log(item))
